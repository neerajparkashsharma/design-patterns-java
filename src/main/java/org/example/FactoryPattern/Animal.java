package org.example.FactoryPattern;

public interface Animal {
    // defining the interface for the animals

    // defining the methods for the animals
    void speak();
    void eat();
    void sleep();
}
